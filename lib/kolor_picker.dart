library kolor_picker;

import 'package:flutter/material.dart';

class KolorPicker extends StatelessWidget {
  const KolorPicker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
